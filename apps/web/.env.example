# GT7 Telemetry Pro - Environment Variables
# Copy this file to .env.local and configure your values

# ===========================================
# APPLICATION
# ===========================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=GT7 Telemetry Pro
NEXT_TELEMETRY_DISABLED=1

# ===========================================
# AUTHENTICATION (JWT)
# ===========================================
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_REFRESH_SECRET=your-refresh-token-secret-key
JWT_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=7d

# Legacy NextAuth support
NEXTAUTH_SECRET=your-super-secret-key-change-in-production
NEXTAUTH_URL=http://localhost:3000

# ===========================================
# CONVEX DATABASE
# ===========================================
# Get these from https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
CONVEX_DEPLOYMENT=your-deployment-name

# ===========================================
# STRIPE PAYMENTS
# ===========================================
# Get these from https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_..., pk_test_...) for development
# Use live keys (sk_live_..., pk_live_...) for production

# API Keys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# Webhook Secret
# For local development, use Stripe CLI: stripe listen --forward-to localhost:3000/api/stripe/webhook
# Get the webhook secret from the CLI output or Stripe Dashboard > Developers > Webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Price IDs (create in Stripe Dashboard > Products)
# Premium Plan: $9.99/month
# - Create a product called "GT7 Telemetry Pro - Premium"
# - Add a recurring price of $9.99/month
STRIPE_PRICE_PREMIUM=price_premium_monthly
STRIPE_PRICE_PREMIUM_YEARLY=price_premium_yearly

# Pro Plan: $19.99/month with 7-day trial
# - Create a product called "GT7 Telemetry Pro - Pro"
# - Add a recurring price of $19.99/month
# - Set up 7-day free trial in subscription settings
STRIPE_PRICE_PRO=price_pro_monthly
STRIPE_PRICE_PRO_YEARLY=price_pro_yearly

# ===========================================
# STRIPE SETUP INSTRUCTIONS
# ===========================================
# 1. Create a Stripe account at https://stripe.com
# 2. Get your API keys from https://dashboard.stripe.com/apikeys
# 3. Create products and prices:
#    - Go to https://dashboard.stripe.com/products
#    - Create "Premium" product with $9.99/month price
#    - Create "Pro" product with $19.99/month price
#    - Copy the price IDs (price_xxx) to this file
# 4. Set up webhooks:
#    - Go to https://dashboard.stripe.com/webhooks
#    - Add endpoint: https://your-domain.com/api/stripe/webhook
#    - Select events: checkout.session.completed, customer.subscription.*,
#      invoice.payment_succeeded, invoice.payment_failed
#    - Copy the signing secret to STRIPE_WEBHOOK_SECRET
# 5. For local development, use Stripe CLI:
#    - Install: https://stripe.com/docs/stripe-cli
#    - Run: stripe listen --forward-to localhost:3000/api/stripe/webhook
#    - Use the provided webhook secret

# ===========================================
# DOCKER DEVELOPMENT
# ===========================================
WATCHPACK_POLLING=true
HOSTNAME=0.0.0.0

# ===========================================
# OPTIONAL: ANALYTICS
# ===========================================
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_MIXPANEL_TOKEN=your-token

# ===========================================
# OPTIONAL: ERROR TRACKING
# ===========================================
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ===========================================
# TEST CREDENTIALS (Development Only)
# ===========================================
# Email: missola@test.com
# Password: master
# Role: premium (full access)